// Countdown now lives at @/components/Countdown so non-embed UI (e.g. the Card
// Rush feed banner) can reuse it. Re-exported here for existing embed imports.
export { Countdown } from "@/components/Countdown";
