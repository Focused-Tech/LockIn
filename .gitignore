# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# firebase
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*
serviceAccountKey.json

# typescript
*.tsbuildinfo

# vercel
.vercel

.vercel

# Firebase Android config (contains project keys; provide per-environment)
android/app/google-services.json

# Fox Pit batch backups
# per-file safety copies taken before a risky edit — working files, never committed
*.bak

# design sandbox deps (never ship)
design-preview/**/node_modules/

# Release signing — NEVER commit these (created by the architect)
keystore.properties
android/keystore.properties
*.jks
*.keystore

# Local slice backups (reported, not committed)
*.bak-keyholder
*.bak-champ
*.bak-locksmith
*.bak-trap
*.bak-fixpass
*.bak-twocode
